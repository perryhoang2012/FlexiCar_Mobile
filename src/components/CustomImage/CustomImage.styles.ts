import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  imgStyle: {
    overflow: 'hidden',
    width: '100%',
    height: '100%',
  },
  container: {
    justifyContent: 'center',
    alignItems: 'center',
  },
  activityIndicatorContainer: {position: 'relative'},
});
